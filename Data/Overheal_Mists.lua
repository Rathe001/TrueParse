-- GENERATED by scripts\fetch-overheal.ps1 - do not edit by hand.
-- Per-spec overheal%% quantiles sampled from WCL report Healing tables
-- (the rankings API has no overheal metric). The engine's overheal
-- adjustment uses these instead of fixed 20/45/60 thresholds: above
-- p90 = -2, above p75 = -1, leaner than p25 = +1. This stub is empty
-- until the first overheal crawl replaces it (run locally — NEVER while
-- a CI crawl slice holds the WCL token); until then the engine falls
-- back to the fixed thresholds.
local _, TP = ...

TP.OverhealCurves = TP.OverhealCurves or {}
